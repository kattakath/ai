# page-lab — measured facts

**A row marked UNVERIFIED is not promoted by editing this table — it is promoted by re-running its
recipe and dating the result.** Editing the date, the wording, or the `Measured` column without
re-running the recipe is a fabrication, not an update. Same discipline as `patterns.md`'s
*Flagged — NOT verified* table and `probes.md`'s *Honest caveats*: epistemic hygiene, **not a backlog**.

## What this file is

Every fact in this plugin that an upstream bump can falsify lives here **once**, with an ID, a date
and a recipe. Everywhere else states the fact in at most one clause and cites `[F-ID]`.

- Citing: write `[F-CDP-29TOOLS]` inline. `checks.<system>.page-lab` fails the build on a `[F-…]`
  citation with no row here.
- Restating: do not. A second prose copy of a volatile fact is the drift this file exists to kill.
- Promoting an UNVERIFIED row: run its recipe, paste the observed result into the `Fact` cell,
  move the row into the measured table, and date it **the day you ran it**.
- `Cited by` is maintained by hand and is a convenience, not an authority — the CI grep is.
- Recipes are written for the **rendered** table: a `\|` in the raw source is a literal `|` in the
  command. Copy from the rendered view, or unescape — an escaped pipe pasted into a shell is a
  literal character, not a pipeline.

## Two harnesses the recipes reuse

Run both under Node 22 (`nix shell nixpkgs#nodejs_22 -c bash`) — the fleet's default Node has no
global `WebSocket` [F-NODE-NO-WS]. Paths are relative to `plugins/page-lab/`.

```bash
# H-MCP — dump a stdio MCP server's tool names.  Usage: h_mcp npx -y <pkg>@<ver> [flags...]
h_mcp() {
  printf '%s\n' \
    '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05","capabilities":{},"clientInfo":{"name":"page-lab-facts","version":"0"}}}' \
    '{"jsonrpc":"2.0","method":"notifications/initialized"}' \
    '{"jsonrpc":"2.0","id":2,"method":"tools/list"}' \
    | "$@" 2>/dev/null | jq -c 'select(.id==2) | .result.tools | map(.name)'
}

# H-CDP — send ONE CDP method to the first page target of a debug browser on :9222.
# Usage: h_cdp Overlay.setInspectMode '{"mode":"none"}'
h_cdp() {
  params="${2:-}"; [ -n "$params" ] || params='{}'
  node --input-type=module -e '
    import { browserSocket, connect, pageTargets, attach } from "./scripts/lib/cdp.mjs";
    const c = await connect(await browserSocket("http://127.0.0.1:9222"));
    const t = (await pageTargets(c))[0];
    const s = await attach(c, t.targetId);
    await c.send("DOM.enable", {}, s);
    await c.send("DOM.getDocument", { depth: 0 }, s);
    console.log(JSON.stringify(await c.send(process.argv[1], JSON.parse(process.argv[2]), s)));
    c.close();
  ' "$1" "$params"
}
```

## Measured facts

| ID | Fact | Measured | How to re-measure | Cited by |
|---|---|---|---|---|
| `F-CDP-29TOOLS` | `chrome-devtools-mcp@1.8.0` exposes exactly **29** tools, identical in attach and launch mode. Upstream's generated `docs/tool-reference.md` describes **~57** from `main`; `--slim` is 3. Absent from the release: the **entire Extensions group** (`install_extension`, `list_extensions`, `reload_extension`, `trigger_extension_action`, `uninstall_extension`), 12 of 13 Memory tools, all PWA, all 3P/WebMCP, `click_at`, `screencast_start/stop`. **Any plan depending on `install_extension` is invalid today** — the clean-room "load a manager into an isolated instance" route does not exist. | 2026-09-06 | `bash scripts/devtools-doctor.sh --verify-tools` — prints `TOOLS_COUNT=` and `TOOLS_DRIFT=`. By hand: `h_mcp npx -y chrome-devtools-mcp@1.8.0 --no-usage-statistics --no-performance-crux` and count the names. | `skills/page-diagnose/SKILL.md`, `skills/page-diagnose/references/tools.md`, `references/pick-protocol.md`, `scripts/devtools-doctor.sh` |
| `F-NO-CDP-PASSTHROUGH` | Nothing in `chrome-devtools-mcp` at any version sends an arbitrary CDP command. A raw-WS side channel is structural, not a stopgap. | 2026-09-06 | `h_mcp npx -y chrome-devtools-mcp@1.8.0 --no-usage-statistics --no-performance-crux` then scan the names for anything protocol-shaped: `grep -i -e cdp -e protocol -e send_command -e raw` — must be empty. | `references/cdp-extras.md`, `references/pick-protocol.md`, `references/routes.md` |
| `F-DOLLAR-ZERO-DEAD` | `$0` is **not** readable from a separate CDP session — `undefined` even with `includeCommandLineAPI:true`. It is bound per-session via `DOM.setInspectedNode`. Do not retry this. | 2026-09-06 | With DevTools open on the tab and a node selected: `h_cdp Runtime.evaluate '{"expression":"$0","includeCommandLineAPI":true}'` → `result.type == "undefined"`. | `references/pick-protocol.md`, `references/cdp-extras.md` |
| `F-INSPECTMODE-WORKS` | `Overlay.setInspectMode({mode:'searchForNode'})` works from a non-DevTools CDP client and `Overlay.inspectNodeRequested` fires on the user's click with a `backendNodeId`. No DevTools window needed. | 2026-09-07 | `page-lab-pick --timeout 20000`, then click any element in the debug browser: exit 0 and exactly one `page-lab/pick@1` object on stdout. | `README.md`, `references/pick-protocol.md`, `references/routes.md`, `scripts/pick-element.mjs` |
| `F-DISARM-HIGHLIGHTCONFIG` | `Overlay.setInspectMode({mode:'none'})` **rejects** with `Internal error: highlight configuration parameter is missing` unless `highlightConfig` is passed on the disarm too. A single `try/catch` around the disarm loop therefore leaves **every** armed tab armed. This is the mechanical root cause of the swallowed-click failure. | 2026-09-07 | `h_cdp Overlay.enable` then `h_cdp Overlay.setInspectMode '{"mode":"none"}'` → the quoted error; repeat with `'{"mode":"none","highlightConfig":{"contentColor":{"r":0,"g":0,"b":0,"a":0}}}'` → success. | `references/pick-protocol.md`, `scripts/lib/cdp.mjs`, `scripts/pick-element.mjs`, `scripts/pick-watchdog.mjs` |
| `F-ARMED-SWALLOWS` | An armed picker swallows the next click on **every** armed tab. | 2026-09-06 | Arm with `page-lab-pick`, then click a link in a tab you did not intend to pick from: the link does not navigate and the click is consumed by inspect mode. | `references/pick-protocol.md`, `references/routes.md`, `commands/pick.md` |
| `F-MULTI-CLIENT` | Chrome has supported multiple simultaneous CDP clients per target since Chrome 63, no configuration. An open DevTools window does not block a second client. | 2026-09-06 | Open DevTools on a tab, then run `page-lab-pick` against the same browser — both stay live. | `skills/page-diagnose/references/attaching.md`, `references/routes.md` |
| `F-TARGETS-13-3` | `/json/list` returned 13 targets, of which only **3** were `type === "page"`. Arm page targets only; arming one tab fails in practice because the operator clicks whichever tab they are looking at. | 2026-09-06 | `curl -s http://127.0.0.1:9222/json/list \| jq '[length, ([.[] \| select(.type=="page")] \| length)]'`. **Recipe needs classic launch-flag mode**: in `chrome://inspect` consent mode every `/json/*` path is 404 and this cannot run as written [F-NO-JSON-HTTP]; the fact itself is NOT re-measured. | `scripts/pick-element.mjs`, `references/pick-protocol.md`, `references/routes.md` |
| `F-NODEID-EPHEMERAL` | `DOM.documentUpdated` invalidates **all** nodeIds and fires on navigation and SPA document replacement. Always re-resolve from `backendNodeId`. | 2026-09-06 | Resolve a nodeId, navigate the tab, then reuse it: `h_cdp DOM.describeNode '{"nodeId":<old>}'` → `Could not find node with given id`. | `scripts/pick-element.mjs`, `references/pick-protocol.md` |
| `F-STICKY-STATE` | Overlay and Emulation state persists across navigation and is **shared with every CDP client including the operator's own DevTools**. Pair every override with its clear. | 2026-09-06 | `h_cdp Emulation.setDeviceMetricsOverride '{"width":390,"height":844,"deviceScaleFactor":3,"mobile":true}'`, let the socket close, then look at the tab and at DevTools' own device bar — still overridden. | `references/cdp-extras.md`, `references/routes.md`, `scripts/lib/cdp.mjs` |
| `F-MCP-SURVIVES-CLOSED-PORT` | `chrome-devtools-mcp` answers `initialize` and stays alive with nothing listening on the port; browser contact is lazy. It does **not** dark the gateway. | 2026-09-06 | Quit every Chromium, confirm `curl -s --max-time 2 http://127.0.0.1:9222/json/version` fails, then run `h_mcp npx -y chrome-devtools-mcp@1.8.0 --browser-url http://127.0.0.1:9222 --no-usage-statistics --no-performance-crux` → the tool list still returns. **Recipe needs classic launch-flag mode**: in `chrome://inspect` consent mode every `/json/*` path is 404 and this cannot run as written [F-NO-JSON-HTTP]; the fact itself is NOT re-measured. | `skills/page-diagnose/references/attaching.md`, `scripts/devtools-doctor.sh` |
| `F-NO-JSON-HTTP` | A browser put into debugging mode from `chrome://inspect/#remote-debugging` serves the CDP **WebSocket** but **not** the `/json/*` HTTP discovery surface — `/`, `/json`, `/json/list`, `/json/version`, `/json/protocol` and `/devtools/inspector.html` all return **HTTP 404**, while `Browser.getVersion` over the WS endpoint from `DevToolsActivePort` succeeds. Measured on **both** Chromium 152 (:9222) and Opera Air OPR/135 (:61867), neither of which had `--remote-debugging-port` in its argv. **This kills `--browser-url` outright**, since puppeteer's `connect({browserURL})` must GET `/json/version` to find the WS URL — the live gateway failed with `Failed to fetch browser webSocket URL from http://127.0.0.1:9222/json/version: HTTP Not Found`. The complement IS now measured and is the opposite: a browser launched the classic way with `--remote-debugging-port` serves `/json/version` and `/json/list` with **HTTP 200** (Opera Air, same build, relaunched with `--remote-debugging-port=61867`). So the 404 is a property of **consent mode**, not of the browser or the version — and `--browser-url` is correct for a launch-flag browser and only for one. | 2026-09-07 | `for p in / /json /json/list /json/version; do curl -s -o /dev/null -w "%{http_code} $p\n" --max-time 4 "http://127.0.0.1:<port>$p"; done` → all 404, where `<port>` is line 1 of `<user-data-dir>/DevToolsActivePort`. Then confirm CDP itself is alive on the WS path in line 2 of that same file. | `modules/shared/mcp.nix`, `skills/page-diagnose/references/attaching.md`, `scripts/devtools-doctor.sh` |
| `F-AUTOCONNECT-USERDATADIR` | `--autoConnect` resolves the endpoint by reading `DevToolsActivePort` from the profile named by **`--userDataDir`**, so it works against any browser and needs no port or UUID configured anywhere. Verified by its own failure path: bare `--autoConnect` errors with `Could not find DevToolsActivePort for chrome at ~/Library/Application Support/Google/Chrome/DevToolsActivePort`, and adding `--userDataDir` redirects that lookup. This is what makes a declarative attach possible in **consent mode**, where the port and the browser WebSocket UUID both change every launch. It is **NOT** a universal answer: against a launch-flag browser the file goes stale and `--autoConnect` attaches to a dead socket [F-DEVTOOLSACTIVEPORT-STALE], so it must be chosen by probing, not assumed. `--wsEndpoint ws://…/devtools/browser/<uuid>` also works but must be re-typed per launch. | 2026-09-07 | `npx -y chrome-devtools-mcp@latest --autoConnect --userDataDir "$HOME/Library/Application Support/com.operasoftware.OperaAir" --no-usage-statistics --no-performance-crux`, then call the page-listing tool → real tabs. Drop `--userDataDir` → the quoted Google/Chrome error. | `modules/shared/mcp.nix`, `skills/page-diagnose/references/attaching.md` |
| `F-DEVTOOLSACTIVEPORT-STALE` | `DevToolsActivePort` line 2 (the browser WebSocket path) is a cached copy the browser does **not** reliably refresh, and a stale line 2 is **dead**, not merely old. Measured on Opera Air relaunched with `--remote-debugging-port=61867`: the file's mtime was `07:08:27` while the browser started `09:09:32` — **never rewritten**. Its `3033135b-…` UUID failed the WebSocket upgrade (`non-101 status code`) while `/json/version` served a live `d58fe459-…`. Consequence: **`--autoConnect` fails against a launch-flag browser** (`Could not connect to Chrome … Cause: Unexpected server response: 404`) even though the browser is up and `--browser-url` attaches fine. Line **1** (the port) stayed correct throughout — it is the trustworthy half. | 2026-09-07 | Relaunch a browser with `--remote-debugging-port=<same port>`, then compare `sed -n 2p <user-data-dir>/DevToolsActivePort` against the path in `curl -s http://127.0.0.1:<port>/json/version \| jq -r .webSocketDebuggerUrl`. Different ⇒ stale; open a WS to each and only the live one upgrades. `bash scripts/devtools-doctor.sh` reports it as `WARN … is STALE`. | `modules/shared/mcp.nix`, `scripts/lib/cdp.mjs`, `scripts/devtools-doctor.sh`, `skills/page-diagnose/references/attaching.md` |
| `F-UGC-NO-VENDOR` | ungoogled-chromium reports `"Browser":"Chrome/152.0.7977.64"` on `/json/version` with **no vendor field**. The build string cannot identify the browser — only the running process path can. | 2026-09-06 | `curl -s http://127.0.0.1:9222/json/version` (no vendor key) versus `pgrep -fl -- '--remote-debugging-port'` (real bundle path). **Recipe needs classic launch-flag mode**: in `chrome://inspect` consent mode every `/json/*` path is 404 and this cannot run as written [F-NO-JSON-HTTP]; the fact itself is NOT re-measured. In consent mode `pgrep` also finds nothing — identify the serving app with `lsof -nP -iTCP:<port> -sTCP:LISTEN` instead. | `scripts/page-route.sh`, `scripts/devtools-doctor.sh`, `skills/page-diagnose/references/attaching.md` |
| `F-UGC-ATTACHES` | `chrome-devtools-mcp` attaches to unsupported Chromium builds despite upstream supporting only Google Chrome / Chrome for Testing. Re-confirmed 2026-09-07 against **Opera Air** (`OPR/135`, Chromium 151): 29 tools and `list_pages` returned its real tabs. The `--browser-url` route in the original recipe is dead [F-NO-JSON-HTTP]; use `--autoConnect` or `--wsEndpoint`. | 2026-09-07 | Point `--autoConnect --userDataDir <profile>` at the unsupported build's user-data dir and call its page-listing tool (name in `skills/page-diagnose/references/tools.md`) — it returns the open pages. | `skills/page-diagnose/references/attaching.md`, `README.md` |
| `F-CASK-NOFLAG` | The fleet browser is a Homebrew cask ⇒ `programs.chromium.package = null` ⇒ upstream's assertion **forbids** `commandLineArgs`. `--remote-debugging-port` **can never be declarative here**; it is a hand-run wrapper. | 2026-09-06 | `src=$(nix eval --raw --impure --expr 'builtins.toString (builtins.getFlake "'"$PWD"'").inputs.home-manager.outPath'); grep -n 'commandLineArgs' "$src/modules/programs/chromium.nix"` → the assertion; and `nix eval .#darwinConfigurations.macos.config.home-manager.users.ismail.programs.chromium.package` → `null`. | `references/routes.md`, `README.md`, `scripts/route-up.sh` |
| `F-KAPTURE-COLD` | Measured default: the Kapture bridge is up on `127.0.0.1:61822` and `/tabs` returns `[]` — zero tabs connected. A running bridge with zero tabs is **dark**, not ready. | 2026-09-06 | `curl -s --max-time 2 http://127.0.0.1:61822/tabs` — `[]` is bridge-up-zero-tabs; connection refused is bridge-down. Two different problems. | `scripts/page-route.sh`, `references/routes.md`, `scripts/route-up.sh` |
| `F-KAPTURE-EVAL-GATE` | `kapture-mcp` **filters `evaluate` out of `tools/list`** until a connected tab has *Allow JavaScript Execution* on (`dist/tool-handler.js:16`), and fires `notifications/tools/list_changed` when it flips. The toggle is **in-memory and resets on disconnect** (`modules/tab-state.js:56`). Never assume `mcp__kapture__evaluate` exists; probe by **name presence**, never by calling it. | 2026-09-06 | Source: `f=$(find "$(npm config get cache)/_npx" -path '*kapture-mcp*' -name tool-handler.js); sed -n '1,30p' "$f"`. Behaviour: check whether `mcp__kapture__evaluate` is in the session tool list with the toggle off, flip it in the popup, check again. | `references/routes.md`, `references/pick-protocol.md`, `scripts/route-up.sh` |
| `F-KAPTURE-POPUP` | Extension 1.2.1 exposes the connect toggle in the **toolbar popup** (`popup.js:31-33`); DevTools is not required. `modules/shared/chromium.nix`'s option description saying otherwise is stale and is fixed by this PR. | 2026-09-06 | Close DevTools entirely, click the Kapture toolbar icon, connect the tab, then `curl -s http://127.0.0.1:61822/tabs` → one entry. Source: grep `popup.js` in the pinned 1.2.1 extension directory for the connect handler. | `references/routes.md`, `README.md`, `modules/shared/chromium.nix` |
| `F-KAPTURE-MUTATES` | `getUniqueSelector()` stamps `id="kapture-N"` or adds class `.kapture-N` on any element Kapture returns. The selector is real, unique, and **evaporates on reload**. Fine to act on; **never** allowed into a `.user.js`. It also dirties the DOM, contaminating any later HTML diff. | 2026-09-06 | Connect a tab, `mcp__kapture__elements({tabId, selector:"h1"})`, then look at the returned selector and at the live DOM: an injected `kapture-N` id/class. Reload — it is gone. | `references/pick-protocol.md`, `references/routes.md`, `scripts/pick-normalize.mjs`, `scripts/userscript-meta-lint.sh` |
| `F-KAPTURE-SKEW` | Server half is `npx -y kapture-mcp@latest bridge` in `~/.claude.json` (imperative, unpinned); extension half is pinned at **1.2.1** in Nix. The bridge already version-gates the extension (`EXTENSION_OUTDATED`, network tools need ≥1.1.0), so skew is a precedented failure. | 2026-09-06 | `jq '.mcpServers.kapture' ~/.claude.json` (unpinned `@latest`) versus `grep -n 'kapture' modules/shared/chromium.nix` (pinned 1.2.1). | `references/routes.md`, `README.md` |
| `F-INAPP-COLD` | `mcp__Claude_Browser__javascript_tool` refuses with *"No preview is open"* until a pane exists; `mcp__Claude_Browser__navigate` with a `url` opens one and the same call then succeeds. **Not probeable from a shell** — it is an in-session MCP tool, like the kapture and cic gates. | 2026-09-07 | Call `mcp__Claude_Browser__javascript_tool` before any `navigate` in a fresh session; then `navigate` to any URL and repeat. There is deliberately no shell command; that absence is the fact. | `references/routes.md`, `scripts/page-route.sh` |
| `F-INAPP-ZERO-VIEWPORT` | A collapsed or hidden in-app pane still executes JS, but the page reports `innerWidth`/`innerHeight` **0** and lays out nothing — a masonry read there returned `columns: 0` with every rect null, which reads exactly like a broken selector. `resize_window` to an explicit size, then reload, before measuring geometry: the same page then returned 4 columns and 8px gaps. | 2026-09-07 | In the in-app browser on any responsive page, evaluate `[innerWidth, innerHeight]` with the pane collapsed, then `mcp__Claude_Browser__resize_window {width:1512,height:900}`, reload, and evaluate again. | `references/routes.md`, `scripts/page-route.sh` |
| `F-CIC-COLD` | `mcp__claude-in-chrome__tabs_context_mcp` answers *"Browser extension is not connected"* by default. **Not probeable from a shell** — its transport is native messaging. | 2026-09-06 | Call `mcp__claude-in-chrome__tabs_context_mcp` in a session with the extension closed. There is deliberately no shell command here; that absence is the fact. | `references/routes.md`, `scripts/page-route.sh`, `scripts/route-up.sh` |
| `F-CIC-REPLMODE` | claude-in-chrome wraps source in a `{ … }` block with `replMode:true`. Top-level `let`/`const` are gone by the next call; all state must hang off `window`. Single evaluation capped ~40 s, output 50 KB. | 2026-09-06 | Two `mcp__claude-in-chrome__javascript_tool` calls: `let probe = 1` then `probe` → ReferenceError; `window.probe = 1` then `window.probe` → `1`. | `scripts/pick-payload.js`, `references/pick-protocol.md` |
| `F-CIC-SANITIZER` | Output is **silently** sanitized, no error, value replaced. String with `=` and (`;` or `&`) → `[BLOCKED: Cookie/query string data]`; `^[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+$` → `[BLOCKED: JWT token]` (**a class chain like `div.card.active` matches exactly**); `^[A-Za-z0-9+/]{20,}={0,2}$` → base64; `^[a-f0-9]{32,}$` → hex credential; key matching `/password\|token\|secret\|api[_-]?key\|auth\|credential\|private[_-]?key\|access[_-]?key\|bearer\|oauth\|session/i` → sensitive key (**a key named `author` trips `/auth/i`**); `cookie`/`cookies` keys blocked; strings >1000 chars truncated; arrays truncated to 100; a top-level array returns only its description; a DOM node returns `[DOM Node]`. | 2026-09-06 | Run the canary object through `mcp__claude-in-chrome__javascript_tool` and compare against the documented substitutions: `({a:'div.card.active', b:'x=1;y=2', c:'<<probe>>', d:'a'.repeat(40)})`. This is the same canary the tier-4 ARM call ships. | `references/pick-protocol.md`, `scripts/pick-payload.js`, `scripts/pick-normalize.mjs` |
| `F-CIC-ORIGIN-RECHECK` | claude-in-chrome re-checks the origin **after** execution: a same-call navigation returns *"Security check failed: the page navigated to a different domain"*. Capture-phase `preventDefault` is mandatory. | 2026-09-06 | On a permitted domain, one `mcp__claude-in-chrome__javascript_tool` call whose body does `location.href='https://example.com'` → the quoted error instead of a result. | `scripts/pick-payload.js`, `references/pick-protocol.md` |
| `F-NODE-NO-WS` | Node v20.20.2 on this fleet has **no global `WebSocket`**; `--experimental-websocket` supplies one; Node 22+ has it unflagged. `packages/page-lab-pick.nix` pins `nodejs_22` so no flag is used in the fleet path. | 2026-09-06 | `node -p 'typeof WebSocket'` → `undefined`; `node --experimental-websocket -p 'typeof WebSocket'` → `function`; `nix shell nixpkgs#nodejs_22 -c node -p 'typeof WebSocket'` → `function`. | `scripts/lib/cdp.mjs`, `packages/page-lab-pick.nix`, `README.md` |
| `F-CRI-UNAVAILABLE` | `chrome-remote-interface` is the canonical off-the-shelf raw-CDP client and is **declined**: adopting it means either an unpinned `npx -y` network fetch at runtime (the precedented `@latest`-skew failure this repo already carries with Kapture) or a nixpkgs attr that no longer exists (`nodePackages` removed). | 2026-09-06 | `nix eval nixpkgs#nodePackages.chrome-remote-interface` → attribute-missing error; `nix search nixpkgs chrome-remote-interface` → no result. | `packages/page-lab-pick.nix`, `references/pick-protocol.md` |
| `F-GATE-HOLE` | `checks.<system>.userscripts` globs `${self}/userscripts/*.user.js` — **this repo only**. nix-personal's `civitai-declutter` shipped with no `@license` and CI stayed green. The private tree must be linted by hand. | 2026-09-06 | `grep -n 'userscripts/\*' flake.nix` → the this-repo-only glob; then `bash scripts/userscript-meta-lint.sh <nix-personal>/userscripts/` and watch it report what CI never saw. | `README.md`, `.claude/skills/userscript-author/SKILL.md` |
| `F-GF-WEBHOOK-LENIENT` | Greasy Fork's webhook path uses `do_lenient_saving`, auto-confirming **every** warning including *version not incremented*. A push that changes code without bumping `@version` is **accepted** and silently never reaches installed users. | 2026-09-06 | Source: `gh search code --repo JasonBarnabe/greasyfork 'do_lenient_saving'`. Behaviour: push a code-only change through the sync webhook and confirm the script's version and install count do not move. | `skills/userscript-author/greasyfork.md` |
| `F-VM-GRANT-OMIT` | Omitting `@grant` is **not** `@grant none`. Omitted ⇒ sandboxed with `GM_info`/`GM.info`/`unsafeWindow` only (VM ≥ 2.32.0); `none` ⇒ sandbox disabled and **no `GM_*` at all**. | 2026-09-06 | Install two throwaway scripts on the same page, one omitting `@grant` and one with `@grant none`, each logging `typeof GM_info` and `typeof unsafeWindow`; compare the two console lines. | `skills/userscript-author/gm-api.md`, `skills/userscript-author/patterns.md` |
| `F-PRESENT-NOT-WORKING` | Geometry and hit-testing prove a control is **present**, never that it **works**. `google-photos-icon-nav` 3.0.0 shipped a search button that was present at 40x40 at (1240,12), on top at its own centre, and completely **inert**: a trusted click opened no field (both `<input>` stayed `0x0` under a `display:none` parent), `document.activeElement` stayed `BODY` and `location.pathname` did not change. It passed `userscript-meta-lint.sh` and every geometry check. This is the whole reason `userscript-acceptance.mjs` exists and why its primitive is the trusted event rather than `el.click()`. | 2026-09-12 | On a wide viewport with a build that lifts the narrow-viewport block: click the collapsed search control via `Input.dispatchMouseEvent` (move/press/release), then read `document.activeElement.tagName` and every `[role="menubar"] input` rect. Inert ⇒ `BODY` and all-zero rects. | `scripts/userscript-acceptance.mjs`, `scripts/lib/harness.mjs`, `skills/userscript-author/SKILL.md` |
| `F-FIRST-TARGET-WRONG` | "The first page target" is the wrong default on a real debug browser: a userscript manager alone contributes two extension page targets beside the page under test. Measured on Chromium 152 (:9222) with one Photos tab open — `/json/list` held **3** page targets, and `selector-verify.mjs` without `--target-id` scored `div:has(> [role="main"])` and `[role="menubar"] > *:has(a[href*="SignOutOptions"])` as **DEAD**, while the same two selectors at the same instant with `--target-id <photos tab>` scored **UNIQUE**. A reader who trusts the default rewrites a working selector. | 2026-09-12 | `curl -s http://127.0.0.1:9222/json/list \| jq -r '.[] \| select(.type=="page") \| .id + " " + .url'`, then run `selector-verify.mjs` with and without `--target-id` on a selector you know resolves. | `scripts/selector-verify.mjs`, `scripts/lib/harness.mjs`, `scripts/userscript-acceptance.mjs` |
| `F-TRANSITION-RACE` | A fixed sleep **races a CSS transition**, and the resulting false negative is indistinguishable from a real bug. Measured on a drawer with `transition: transform .18s ease-out`, injected at `readyState: "complete"`: the nav read `x = 0` at 200 ms and `x = -88` at 800 ms, so an assertion at 200 ms reported "drawer not hidden" for a drawer that was hiding correctly. Two equal reads are **not** proof either — an account popover read the same height twice mid-growth (210 px) before settling at 622 px, which fired a "popover covered" assertion against a popover that was simply still opening. Poll until stable, and raise `stableFor` for anything that grows in steps. | 2026-09-12 | Inject a script that applies a transform under a transition, then sample the target's `getBoundingClientRect().x` at 200 / 800 / 2500 ms — the first sample differs from the last two. | `scripts/lib/harness.mjs`, `scripts/userscript-acceptance.mjs` |
| `F-CONTROLLED-INPUT` | A framework-**controlled** `<input>` ignores the three obvious ways to clear it, and a strict equality assertion against one accumulates text across runs. Measured on Google Photos' search field: `i.value = ''` was reverted; the platform select-all chord (`Meta`/`Ctrl`+`a`) was swallowed, leaving the text intact; and characters dispatched as `char` events landed at the **caret**, not appended (`"dogdogd|ogdog"`-style interleaving). The native `HTMLInputElement.prototype.value` setter plus a bubbling `input` event is the path the framework does honour. Photos also ships a **disabled decoy** input beside the live one, so "the widest input" targets the wrong node and reports a working field as broken. | 2026-09-12 | Type into such a field twice via `Input.dispatchKeyEvent {type:'char'}` and read `.value` between runs — it grows and interleaves. Then assign `''` directly (reverted) versus the native setter + `new Event('input',{bubbles:true})` (accepted). | `scripts/lib/harness.mjs`, `scripts/userscript-acceptance.mjs` |
| `F-ANIMATION-BEATS-IMPORTANT` | A **running animation outranks `!important`**. CSS animations and Web Animations sit in their own cascade origin above author-important declarations, so a site that animates a property owns it outright and a userscript's `!important` on that same property is silently ignored — no error, no warning, just a computed value that is not yours. Measured 2026-09-12 on Google Photos' nav: `getAnimations()` returned one **running** effect whose keyframes carry `transform`, and `transform:translateX(-110%)!important` computed to `matrix(1, 0, 0, 1, 0, 0)` while **every other declaration in the very same rule applied** (position, inset, z-index, overflow, visibility all took). The drawer therefore sat wide open across the content — a mangled page, not a degrade to stock. The fix is to stop competing: `translate` / `rotate` / `scale` are **independent longhands** that compose with `transform` rather than overriding it, so an animation on `transform` cannot touch them — `translate:-110% 0!important` gave x=-282 closed and x=0 open with their animation still pinning `transform` to identity. Cancelling the animation is the wrong fix; the site restarts it. | 2026-09-12 | On the target element: `getComputedStyle(el).transform` versus your rule, then `el.getAnimations().map(a => [a.playState, a.effect.getKeyframes().flatMap(Object.keys)])`. A running effect listing the property you are fighting is the diagnosis. Re-express the rule with the independent longhand and re-read the computed value. | `skills/userscript-author/patterns.md`, `references/facts.md` |
| `F-CHROMA-NOT-HSL` | **HSL saturation is the wrong neutrality test for a colour remap; chroma (`max-min` of the RGB channels) is the right one.** A remap that decides "is this a neutral to invert, or an accent to preserve" on HSL saturation misclassifies near-white and near-black, because HSL saturation is normalised by lightness and explodes at the extremes. Measured 2026-09-06 on a classifieds listing site: the page ground `rgb(252, 252, 248)` computes **HSL saturation 0.40** — well past any sane accent threshold — so the remap preserved it as a brand colour and the cream page came back **olive, `rgb(93, 93, 40)`**. The same colour's chroma is `(252-248)/255 = 0.016`, unambiguously neutral. Switching the test to chroma with a threshold around 0.12 classified every ground correctly on the same page. | 2026-09-06 | For a suspect colour, compute both: HSL saturation via any converter, and `(max(r,g,b) - min(r,g,b)) / 255`. Near-white and near-black grounds are the discriminating cases — a colour the eye reads as white or grey with HSL saturation above your accent threshold reproduces the bug. | `skills/site-redesign/theming.md` |
| `F-NOT-TAKES-MAX-SPECIFICITY` | **`:not()` takes the specificity of its most specific argument**, so a "flatten everything" rule written as `:not(#a, .b)` carries **ID weight**, not zero. Consequence for a colour remap, measured 2026-09-06 on a classifieds listing site: colours the remap *declined* to map still lost, because the broad flatten rule outscored the site's own declaration that was supposed to survive. Merely **skipping** a colour does not preserve it — a preserved colour must be **re-declared** at equal-or-higher weight by the remap itself. The same arithmetic makes `:where()` the opposite tool: it contributes zero. | 2026-09-06 | Write `html:not(#nope) .x { color: red }` and a plain `.x { color: green }` in a later sheet; the `:not()` rule wins despite `#nope` matching nothing. Then confirm `:where(#nope)` in the same position loses. | `skills/site-redesign/theming.md` |
| `F-CONTRAST-RATIO-ONLY` | **Key an automated ink repair on the measured contrast ratio alone — never on a "the background is light" gate.** A lightness gate silently skips mid-tones, which are exactly the cases that need repair. Measured 2026-09-07 on a classifieds listing site: a strip whose background computed to relative luminance **L = 0.35** fell outside a "light background" branch, so the repair left dark ink on it and the ratio went **2.49 to 1.80 — worse than before the theme**. Repairing on ratio only, and picking whichever of the two inks *measures* better against that specific background, fixed it without a lightness branch at all. | 2026-09-07 | Render text on a background at relative luminance ~0.35, run your repair, and read the resulting contrast ratio. A repair with a lightness gate leaves it unchanged or lowers it. | `skills/site-redesign/theming.md` |
| `F-BACKDROP-ADJACENCY` | **Many sites pair a dialog with its backdrop by DOM position, so relocating the dialog's trigger leaves the backdrop hidden and the dialog undismissable by pointer — while Escape keeps working, which disguises the bug as an event-trapping problem.** Measured 2026-09-13 on a classifieds listing site: opened from a relocated trigger, the dialog rendered at 480x664 with `body.modal-open` set, and **all three of the site's backdrops stayed at `display: none`**; a trusted click anywhere outside did nothing. Forcing one backdrop visible by hand and clicking it closed the dialog and cleared the class, proving dismissal is the site's own backdrop handler. Opened from the stock trigger in its original position, the same backdrop showed at 1512 px. Each backdrop immediately **follows** its modal in the DOM, so an adjacent-sibling rule shows exactly one scrim, the correct one, and can never stack two. | 2026-09-13 | Open the dialog from the relocated trigger, then read `getComputedStyle(b).display` for every backdrop-ish node. All `none` while the dialog is open is the diagnosis. Confirm by setting one to `block` and dispatching a trusted click on it. | `skills/site-redesign/relocation.md` |
| `F-TRANSFORM-CONTAINING-BLOCK` | **A transformed ancestor becomes the containing block for its `position: fixed` descendants**, so sliding a panel off-canvas with `transform` silently re-anchors every fixed control inside it — the "fixed" toggle button stops being fixed to the viewport and rides the panel off-screen. Measured 2026-09-02 on a classifieds listing site. Off-canvas via a `right` offset, or via the independent `translate` longhand on a node that contains no fixed descendants, avoids it entirely. `filter`, `perspective`, `backdrop-filter`, `contain: paint` and `will-change` on those properties do the same thing. | 2026-09-02 | Put `position: fixed; top: 0; right: 0` inside a `transform: translateX(-100%)` ancestor and read the child's `getBoundingClientRect()`. It tracks the parent, not the viewport. | `skills/site-redesign/relocation.md` |
| `F-DISCRETE-NEEDS-BOTH` | **Fading an element that toggles `display` needs BOTH `transition-behavior: allow-discrete` AND an `@starting-style` rule — with only one, the transition is skipped silently.** `display` is not an animatable property, so without `allow-discrete` the element snaps; and an element entering from `display: none` has no previous computed style to interpolate from, so without `@starting-style` the entry transition has no start value and is dropped. Measured 2026-09-13 on a classifieds listing site: dialogs and their scrim snapped in with one of the two present and faded correctly only with both. No console warning is emitted in either failure. | 2026-09-13 | Write `.d { display:none; opacity:0; transition: opacity 300ms, display 300ms allow-discrete }` with and without a matching `@starting-style { .d[open] { opacity: 0 } }`, toggle it, and watch for a fade versus a snap. | `skills/site-redesign/theming.md` |
| `F-SRC-NOT-LOAD` | **An image fade keyed on the presence of `[src]` plays against an empty box; key it on a marker set in the `load` handler instead.** `src` is assigned before a single byte has decoded, so a `[src]`-triggered opacity transition runs its full duration on nothing and the picture still pops in at the end — the exact artefact the fade was added to remove. Measured 2026-09-13 on a classifieds listing site: moving the trigger to `img.addEventListener('load', ..., { once: true })` setting a dataset marker produced the intended fade. Under `prefers-reduced-motion: reduce` the fade must resolve to `opacity: 1`, not to an invisible image. | 2026-09-13 | Throttle the network, apply a `[src]`-keyed fade to a large image, and watch: the box fades in empty, then the image appears at full opacity. | `skills/site-redesign/theming.md` |
| `F-CSS-SHADOWS-GLOBAL` | **A script-scope `const CSS` shadows `window.CSS`**, so `CSS.escape(...)` elsewhere in the same IIFE throws `Cannot access 'CSS' before initialization` at evaluation time — a TDZ error, not a "not a function" error, which sends debugging in the wrong direction. Measured 2026-09-11 on a classifieds listing site, where the stylesheet template literal was named `CSS`. Renaming it (to `SHEET_CSS`) was the whole fix. The same trap exists for `Element`, `Image`, `Event`, `URL`, `Response` and any other global a script might reasonably reuse as a local name. | 2026-09-11 | In one IIFE write `const CSS = 'x'` after a line that calls `CSS.escape('y')` and evaluate it. | `skills/site-redesign/SKILL.md` |
| `F-ADOPTED-SORTS-LAST` | **`document.adoptedStyleSheets` sort AFTER all document stylesheets of the same origin and layer**, so a constructable sheet adopted by a userscript wins ties against the site's own `<style>`/`<link>` rules at equal specificity without needing `!important`. Measured 2026-09-05 on a classifieds listing site. This is ordering, not weight: a site rule with higher specificity, or any `!important`, still wins, and a running animation still outranks both [F-ANIMATION-BEATS-IMPORTANT]. | 2026-09-05 | Adopt a constructable sheet declaring `.x { color: red }` and add a document `<style>` with `.x { color: green }` after it. Red wins. | `skills/site-redesign/theming.md` |
| `F-RAF-SURVIVES-TEARDOWN` | **A `requestAnimationFrame` callback scheduled before teardown still fires after it, and rebuilds exactly what teardown just removed.** Measured 2026-09-10 on a classifieds listing site: teardown removed the injected panel, a frame already queued by a coalescer then ran and re-created it, leaving an orphan panel with no controller behind it — and the next injection made a second one. A cancel list is not enough on its own, because the callback may already be executing; the durable fix is a `torn` flag checked at the top of **every** coalescer, builder and scan, plus ordering teardown so it stops new work **before** it undoes the DOM. The same applies to `setTimeout`, `queueMicrotask`, and a resolved `fetch` continuation. | 2026-09-10 | Schedule a `rAF` that appends a node, run teardown synchronously in the same tick, and check the DOM on the next frame. | `skills/site-redesign/SKILL.md` |
| `F-GRID-TRACKS-NOT-WIDTHS` | **When a relocated block overflows its new container, suspect the grid track list before the widths.** Measured 2026-09-08 on a classifieds listing site: a control block moved into a 383 px rail overflowed, and every width on it was already `auto` or percentage — the overflow came from an inherited `grid-template-columns: 410px 409px 108px`, which no width override can shrink. `grid-template-columns: minmax(0, 1fr)` plus `min-width: 0` on the items fixed it. A second instance on the same page was **margins**, not widths either: 9 px either side of a word plus 17 px before a chevron summed to 400 px in the same 383 px column. | 2026-09-08 | On the overflowing node read `getComputedStyle(el).gridTemplateColumns` and sum the track list; compare it to the container's content box. A sum exceeding the box with no explicit width set is the diagnosis. | `skills/site-redesign/relocation.md` |
| `F-IO-BACKGROUND-TAB` | **`IntersectionObserver` does not deliver entries in a background tab**, so any acceptance spec that drives lazy enrichment stalls with no error and reports a false negative against working code. Measured 2026-09-09 on a classifieds listing site: an enrichment queue driven by an observer processed zero items until the tab was activated, then drained normally. Give the target a live clock at the top of any spec whose subject is observer-driven — `Emulation.setFocusEmulationEnabled` is the remedy to reach for, not `Target.activateTarget` [F-FOCUS-EMULATION] — and poll until the value stops changing rather than sleeping [F-TRANSITION-RACE]. | 2026-09-09 | Run an `IntersectionObserver`-driven spec against a tab that is not frontmost, then re-run it after activating the target. | `skills/site-redesign/acceptance.md` |
| `F-INPUT-SILENTLY-DROPPED` | **A CDP page target can silently stop delivering `Input.dispatchMouseEvent` press/release while still delivering `mouseMoved`** — so every control measured through it reads as "renders correctly, does nothing", and a redesign gets blamed for a dead rig. Measured 2026-09-13 on a long-lived target shared by three concurrent agents: a **trusted click on a plain, stock link, before any injection, registered ZERO click events**; `dispatchTouchEvent` timed out at 30 s on the same target; a freshly created target navigated on the identical click. Every null result from such a tab is unsound, including a whole survey section that concluded a site's menus were broken. **Assert the rig before trusting any null result**: dispatch a trusted click at a known-good stock control and require the observable effect, then measure. Prefer one fresh target per phase over a shared long-lived one. | 2026-09-13 | On the target under test, before injecting anything, trusted-click a plain `<a href>` and assert navigation (or a counted `click` listener). No effect means the rig is dead, not the page. | `skills/site-redesign/survey.md`, `skills/site-redesign/acceptance.md` |
| `F-BG-TAB-FREEZES-ANIM` | **A backgrounded tab freezes the animation clock, which breaks measurement in two ways that both look like product defects.** (1) A CSS transition never ticks, so the element reads its **stock start value forever** — and `settle()` reports that as *stable*, because it genuinely never changes; the tool that exists to prevent false negatives actively confirms the wrong answer. (2) **A frozen transition pins its property ABOVE author-`!important`**: `opacity: 1 !important` could not move an element, and `el.getAnimations()` showed two stuck transitions. This is [F-ANIMATION-BEATS-IMPORTANT] in a form that fact does not cover — the animation is not *running*, it is **frozen**, and still outranks. Measured 2026-09-13 by two independent agents on one site, presenting as three unrelated symptoms (colours "unreached", hover reveal dead, clicks not navigating); `Target.activateTarget` cleared all three at once — but activation is **not** the remedy to ship, because `Emulation.setFocusEmulationEnabled` clears the same three without stealing the operator's window [F-FOCUS-EMULATION]. The tell for (1): the surviving element list was **exactly the motion block's selector list**. `bringToFront` is insufficient when several agents share a browser and each keeps calling it — and so is activation, for the same reason. For colour measurement, adopt a measurement-only `*{transition:none!important;animation:none!important}` sheet — emulating `prefers-reduced-motion` is **not** enough, because a site may ship its own unguarded transition class. | 2026-09-13 | Put a transitioned element on a page, background the tab, and read its computed value repeatedly — it stays at the start value and `settle()` calls it stable. Then try to override that property with `!important` and read it back. | `skills/site-redesign/acceptance.md`, `skills/site-redesign/theming.md` |
| `F-CSS-COMMENT-NESTING` | **CSS comments do not nest: a `/* … */` marker written inside a comment ends the OUTER comment at its first `*/`, and the parser then consumes the following block as comment text — silently.** Measured 2026-09-13: a guard marker written as a nested comment inside a stylesheet's header comment caused the entire palette block to be swallowed, so every `var(--token)` resolved to `unset` — black text on a white canvas, **no console error, no parse exception, `cssRules.length` simply lower than written**. The failure presents as a theme that "did not apply" rather than as a syntax error. Guard against it by asserting after `replaceSync` that the expected token count resolves (`getComputedStyle(root).getPropertyValue('--…')` non-empty for each) before measuring anything downstream. | 2026-09-13 | Write `/* outer /* inner */ .x{color:red} */` into a constructable sheet, `replaceSync` it, and compare `cssRules.length` against the rules you intended. | `skills/site-redesign/theming.md` |
| `F-MEDIA-GATED-CLASS-INERT` | **A utility class whose declaration lives inside a media query is INERT outside that query, so toggling the class does nothing and the "reuse the site's own switch" shortcut silently fails.** Measured 2026-09-13 on a video site whose mobile drawer is hidden by `.mobile-hide`: that class's `display:none` sits inside `@media (max-width:991px)`, so removing it at a 1512 px viewport changed **nothing** — boxes byte-identical before and after. The panel was already visible at desktop; it *is* the desktop header. Making the site's own class usable as a switch requires **re-emitting its rule un-gated** (and, here, also un-gating a second base rule, `.mobile-show{display:none}`, which was what actually hid the 174 links: 1 visible link closed, 179 open). The general lesson: before treating a site class as a lever, verify the rule that gives it meaning is **in scope at the viewport you intend to use it at** — a class is not behaviour, the rule is. | 2026-09-13 | Toggle the candidate class at your target viewport and diff the element's `getBoundingClientRect()` and computed `display`. No change means the declaration is gated out of scope. | `skills/site-redesign/relocation.md`, `skills/site-redesign/survey.md` |
| `F-INJECT-IS-NOT-INSTALL` | **Evaluating a userscript's IIFE into a loaded page tests a world the installed script never sees, and an acceptance suite built that way can report zero failures while every reported defect is live.** The installed script runs at `@run-at document-start`: no `<body>`, no site JS yet, and the site's own scripts run *after* it and may overwrite what it did. An `eval` at `readyState: "complete"` inverts that order entirely. Measured 2026-09-13: a suite of **88 injected checks reported 0 failures** on a build that, once installed, applied its dark theme to only four of six page shapes, rendered one gallery completely blank, and opened a drawer whose contents laid out at 55px wide by 5132px tall; **six probes at document-start found all five defects in minutes**. Verify with `Page.addScriptToEvaluateOnNewDocument`, which runs at document-start on every navigation, and drive real navigations rather than one `eval` into a settled page. Beware also that the operator's genuinely installed copy is running at the same time: two copies in one document produce doubled UI counts that look like a teardown bug and are not. | 2026-09-13 | Take any behaviour that depends on running before the site's JS, verify it by `eval` into a loaded page (it passes), then re-verify via `Page.addScriptToEvaluateOnNewDocument` on a fresh navigation. | `skills/site-redesign/acceptance.md`, `skills/site-redesign/SKILL.md` |
| `F-RAN-AGAINST-THE-OPERATORS-BROWSER` | **A verification runner that defaults to `127.0.0.1:9222` lands in the operator's own browser, where a userscript manager is holding an INSTALLED — and usually OLDER — copy of the very script under test; every verdict then describes the gap between the installed build and the file on disk.** Measured 2026-09-13: `redesign-acceptance.mjs` was invoked with `PL_BROWSER_URL` pointing at a throwaway browser, ignored that variable, attached to `:9222` instead, performed four real navigations in the operator's window (their tabs, their Violentmonkey, an install-confirm page open at the time) and returned **8 failures across 4 shapes** — `#toptopbel 1512x36`, `.blogPost 726x125` ×4, `.bottomrelated 1512x21`, `#vidcontent 1512x232`. All six were blocks the version on disk removes and the installed version did not. Direct measurement on the intended browser returned `display: none`, `0x0` for every one. Two defects, not one: the default, and the silently-ignored env var. The runner now REFUSES without `--browser-url`/`PL_BROWSER_URL`, and preflights one un-injected navigation asserting the root flag is absent. | 2026-09-13 | Install a script, then run an acceptance suite against that browser with an edited copy on disk. The suite reports the edits as defects. Re-run against a browser with no manager. | `scripts/redesign-acceptance.mjs`, `skills/site-redesign/acceptance.md` |
| `F-ISOLATED-COEXISTS` | **A Chromium launched with its own `--user-data-dir` on its own debugging port coexists with an already-running Chromium; the "flag is read at startup only" trap applies to the SAME PROFILE, not to the machine.** Measured 2026-09-13 on macOS 26.6 / Chromium 152: with the operator's browser live on `:9222`, `open -na Chromium.app --args --remote-debugging-port=9345 --user-data-dir=$(mktemp -d)` produced a second main process that answered on `:9345`, while `:9222` kept all **17** of its targets. `route-up.sh --isolated` nevertheless refused, printing `BLOCKED: a Chromium is already running WITHOUT --remote-debugging-port` — a claim it never tested, against a process whose own printed command line carried `--remote-debugging-port=9222` — and advising `Cmd-Q`, which targets the operator's own session. `--isolated` is now exempt from the trap, and the non-isolated message reports the port it actually found. | 2026-09-13 | With a browser already running, `open -na <bundle> --args --remote-debugging-port=<free port> --user-data-dir=$(mktemp -d)`, then poll both ports. | `scripts/route-up.sh` |
| `F-COLD-PROFILE-BOOTS-SLOWLY` | **A throwaway `--user-data-dir` is a COLD profile and takes appreciably longer than 10s to answer on its debugging port, so a 10s readiness budget reports DOWN on a launch that worked.** Measured 2026-09-13: an isolated Chromium did not answer within a 16s poll window, and answered when re-checked shortly after; `ps` confirmed the process and its `user-data-dir` throughout. `route-up.sh` now allows 40s for `--isolated` and keeps 10s for a warm profile. Do not conclude a launch failed from one short poll — check `ps` for the process before retrying, or a retry launches a second instance. | 2026-09-13 | Launch with a fresh `mktemp -d` profile and poll `/json/version` every second, recording the first second it answers. | `scripts/route-up.sh` |
| `F-INSIDE-THE-GRID-IS-NOT-A-CARD` | **A leftover sweep that asks "is this inside a keeper?" is structurally blind to non-unit CHILDREN of the surface container — a grid container's children are not all cards.** Measured 2026-09-13 on eporner at 1512: `p.catheadtext` (1512x40 of marketing prose) was a direct child of `#rec-best-vid`, one of the page's five grid containers, and laid out as a grid ITEM beside the cards. It survived **four** successive leftover sweeps, each of which skipped anything a keeper contained. Enumerating non-unit direct children across shapes showed a class rather than an instance: prose on the index, an `h5` plus a float clearer on the sort shape, and `div.seheader` x5 at 79px each on a profile — **395px of headers on one page**. Float clearers inside a `display:grid` container are inert and go with the rest. The fix hides by ELIMINATION and therefore must be GATED on the container actually holding a unit (`:has(> div.mb[data-id])`), or a renamed unit selector turns "every child that is not a card" into "every child" and blanks the grid. | 2026-09-13 | For each surface container, list `[...g.children].filter(k=>!k.matches(cardSelector))` and read each one's computed display and box. Compare against what the leftover sweep reported. | `scripts/lib/redesign-checks.mjs`, `skills/site-redesign/keep-list.md` |
| `F-SURVEY-HAD-NO-TOOL` | **The survey's most mechanical measurements (M1-M4, M10-M12) shipped as PROSE while acceptance shipped a 14-group runner, so every new site hand-rolled its recon — and the hand-rolled version got the primary container wrong on the second URL shape it looked at.** Measured 2026-09-13: an ad-hoc ranking by "children that contain a link" selected `div.premium-results-line` (7 children, dense with links) over the real grid `div.mozaique` (40+ children) on a search shape. `keep-list.md` warns about exactly this, and prose cannot enforce a ranking. `survey-recon.mjs` ranks by children carrying a UNIT link, considers only containers that RENDER, and reports organic share, href shapes, hover autoplay under a trusted pointer, pagination, per-width unit geometry and stock-theme signals. | 2026-09-13 | Rank candidate containers by raw link count on a search page carrying a promoted strip; compare against ranking by unit-link-bearing children. | `scripts/survey-recon.mjs`, `skills/site-redesign/survey.md` |
| `F-LAZY-GRID-UNDER-REPORTS` | **A grid measured at settle, without scrolling, reports roughly half itself — so container ranking, organic share and unit geometry are all computed from a partial DOM.** Measured 2026-09-13 on xhamster: video links numbered **66** at settle and **109** after one scroll pass of the document, with `scrollHeight` growing 4168 to 4368. The first run of the recon tool found NO qualifying container on five shapes of that site; scrolling first was one of the two fixes (the other was an href heuristic that did not match `/videos/`). Scroll before measuring, always. | 2026-09-13 | Count `a[href]` matching the unit pattern at settle, scroll the document in steps, and count again. | `scripts/survey-recon.mjs` |
| `F-A-RAIL-PASSES-TWO-OF-THREE` | **A related-videos carousel satisfies two of the three qualifying signals — its cards link to videos and preview on hover — so a gate built on those two alone selects a RAIL as the page's primary surface.** Measured 2026-09-13 on xhamster: ranking rendering containers by children carrying a unit link picked `div.scrollable-<hash>` on three of five shapes, each scoring an organic share of **1.00**, because a rail is all units and nothing else. The real grid was `div.thumb-list--sidebar` at **15 rows / 54 children**. The separating measurement is ROWS: count distinct child top-offsets and require at least two, and treat a horizontally scrolling container (`overflow-x: auto|scroll` with `scrollWidth > clientWidth`) as a rail regardless. With the test in place the same five shapes selected the real grid, and `/photos` correctly stopped qualifying with the reason named. | 2026-09-13 | Rank containers by unit-link-bearing children on a page carrying a carousel; compare the winner's distinct child top-offsets against the real grid's. | `scripts/survey-recon.mjs`, `skills/site-redesign/keep-list.md` |
| `F-GENERATED-CLASS-ROTS-IN-MINUTES` | **A hashed framework class can stop matching between two loads of the same page MINUTES apart — not merely across deploys.** Measured 2026-09-13 on xhamster: a container reported as `div.scrollable-7fed9` by one run returned `null` from `document.querySelector('div.scrollable-7fed9')` on the next run in the same session. Sibling hashes differed too (`container-c9dbd`, `root-b7566`, `root-16b1c`, `insideVideoList-b7566`). This is the empirical case for the no-generated-class rule at its strongest: such a selector can rot before the survey that produced it is finished. Prefer a role, an `href` shape or a data attribute, and break ranking ties AGAINST a hashed candidate. | 2026-09-13 | Record a hashed class from a rendered container, reload, and re-query it. | `scripts/survey-recon.mjs`, `scripts/selector-verify.mjs` |
| `F-HOVER-PROBE-NEEDS-A-RIG` | **A hover-preview probe without a rig assertion produces a false NO and a false YES on the same site, in the same run.** Measured 2026-09-13 on xhamster. False NO on the index: the probe moved a trusted pointer to the first card's centre and saw no preview — but `elementFromPoint` at that point returned `DIV.root-16b1c`, an overlay covering the card, and `unit.matches(':hover')` was **false**; the pointer never arrived. False YES on `/newest`: the probe searched the whole GRID for a playing `<video>` and found one in a different card (`anyVideoOnPage: 4`) while `:hover` on the probed unit was likewise **false**. Both verdicts were reported confidently, and hover autoplay is one of the three signals that decide whether a page is in scope at all. Three conditions before any verdict: `elementFromPoint` must land inside the unit, `:hover` must set on it, and the preview must be sought INSIDE that unit. Failing the first two is UNMEASURED with the blocker named — never NO. | 2026-09-13 | Hover a card behind a sticky overlay and read only for a video in the container; then read `elementFromPoint` and `matches(':hover')` at the same point. | `scripts/survey-recon.mjs`, `skills/site-redesign/survey.md` |
| `F-TRANSPARENT-IS-NOT-BLACK` | **`getComputedStyle(body).backgroundColor` is routinely `rgba(0, 0, 0, 0)`, and a naive digit-scrape of that string yields three zeroes — black — so a WHITE site is reported as shipping a stock DARK theme.** Measured 2026-09-13 on xhamster: every one of five shapes returned `bodyBg rgba(0, 0, 0, 0)`, luminance `0`, verdict `stockDark: true`, on a light page. M12 is the measure that can invalidate a redesign plan outright, so this false positive is expensive in exactly the wrong direction. Walk to the first ancestor whose background has alpha above ~0.05 and fall back to white, which is what the viewport paints when nothing else does. | 2026-09-13 | Read `getComputedStyle(document.body).backgroundColor` on any site that paints its ground on `html` or a wrapper, and compute luminance from the raw digits. | `scripts/survey-recon.mjs`, `skills/site-redesign/survey.md` |
| `F-BACKTICK-IN-A-COMMENT-CLOSES-THE-LITERAL` | **A backtick inside a comment that sits within a JS template literal terminates the literal, and the resulting `SyntaxError` points at the prose, not at the real cause.** Hit TWICE on 2026-09-13 in one session: once in a userscript's CSS template (`SyntaxError: Unexpected token '*'`, reported at a line of explanatory comment) and once in a CDP expression string (`Unexpected identifier 'body'`). Both were comments quoting an identifier in backticks, inside `= \`...\``. Style guidance and code-quoting habits push toward backticks precisely where they are fatal. Write such comments with plain words, and run `node --check` after every edit that touches a template literal. | 2026-09-13 | Put a backtick-quoted word in a comment inside a template literal and run `node --check`. | `scripts/survey-recon.mjs` |
| `F-PROGRAMMATIC-FOCUS-IS-NOT-A-KEYBOARD-USER` | **Driving an autohiding bar's keyboard check with `el.focus()` reports "keyboard path is dead" on scripts whose keyboard path works, because a correct bar may gate its focus reveal on the reader having ACTED and `el.focus()` produces no keydown.** The gate is not defensive coding: a site that autofocuses a control inside the bar (eporner autofocuses `#srch` inside `#top2`) makes a `:focus-within` rule true from load, so the bar never hides — measured, and the reason that script drives the reveal from `focusin` behind a `userActed` flag set by a real `keydown`/`pointerdown`. Measured 2026-09-13: the topbar group's bare `el.focus()` produced 4 false failures across 4 shapes (`shown:false, top:-90, h:90`) on a build whose reveal fires correctly for any actual Tab press. Send a TRUSTED `Tab` before reading the focus route — the suite's own "trusted events only" rule, previously broken inside the suite. | 2026-09-13 | On a bar that gates its reveal on user action, read the revealed state after `el.focus()` alone (dead), then after a trusted `Input.dispatchKeyEvent` Tab followed by focus (revealed). | `scripts/lib/redesign-checks.mjs`, `skills/site-redesign/topbar.md` |
| `F-CLAMP-LIVES-ON-THE-TEXT-NODE` | **Reading `-webkit-line-clamp` off the overlay node reports `none` on a build whose clamp works, because the common overlay shape is a gradient WRAPPER with the text in a child.** Measured 2026-09-13 on eporner: the overlay is the site's own `div.mbunder` — positioned, `opacity: 0`, gradient background — and the clamp is two levels down on `p.mbtit > a`. Reading only the wrapper produced 4 false failures (`{"got":"none"}`) across 4 shapes. The same shape also makes the naive `hiddenAtRest` anchor wrong in the other direction: the inner text node carries `opacity: 1 !important` by design, so pointing the config at it reports `opacity: 1` at rest. Read the clamp from the overlay OR its first clamped descendant, and point `overlay.selector` at the node that is positioned and faded. | 2026-09-13 | On a gradient-wrapper overlay, read `getComputedStyle(overlay).webkitLineClamp` (`none`) and then the same property on its text descendant (the real value). | `scripts/lib/redesign-checks.mjs` |
| `F-A-BREAK-THAT-ADDS-CANNOT-BREAK-A-PRESENCE-TEST` | **A degradation harness that breaks an anchor by SETTING an attribute cannot break an anchor written as a presence test, so the group silently never runs.** `[data-id]` matches on presence: setting it to `""` leaves it matching, the script applies normally, and every degradation assertion passes for the wrong reason. Measured 2026-09-13: `breakAttr: 'data-id', breakValue: ''` against a script anchored on `div.mb[data-id]` reported `the break actually broke the anchor — FAIL {gridOn:"", themed:true}`, which is the harness correctly saying it had not tested anything. Removal is the other half of the mechanism, not an option; `degrade.removeAttr` now covers it. | 2026-09-13 | Point `degrade` at a presence-test anchor and break it by setting the attribute empty. The script still applies. Re-run with `removeAttr`. | `scripts/lib/redesign-checks.mjs`, `scripts/redesign.config.example.mjs` |
| `F-ZERO-SIZE-CONTROL-DOES-NOT-TAKE-FOCUS` | **`el.focus()` on a zero-size element does not move focus, and a focus check that then reads only `bar.contains(document.activeElement)` reports whatever focus already was — a trivial pass on one element and a confident false failure on the next.** Measured 2026-09-13 on eporner: the first four `#top2 a[href]` are `display: inline`, `visibility: visible`, box **0x0** (icon toggles collapsed at desktop). Focusing `#menuslide` left `document.activeElement` unchanged; because the site autofocuses `#srch` — which is also inside `#top2` — a naive containment read returned `inBar: true` anyway. One shape later the same read returned `inBar: false, active: "A"`, reported as a dead keyboard route on a bar whose keyboard route works. Walk every match and require the element to BECOME `activeElement`. | 2026-09-13 | Focus a `0x0` but displayed link and compare `document.activeElement` before and after. | `scripts/lib/redesign-checks.mjs`, `skills/site-redesign/topbar.md` |
| `F-ASPECT-RATIO-IN-AUTO-ROW` | **An `aspect-ratio` on a grid item's CHILD collapses in a content-sized row: sizing the row asks the item for its intrinsic height, which resolves the child's ratio against an INDEFINITE width and returns a near-empty box.** Measured 2026-09-13: a card whose `.thumb-inside` carried `aspect-ratio: 16/9` and laid out correctly at 299x168 sat in a track that sized itself to **35px**, and the card's own `overflow: hidden` then clipped the content away — a gallery of 98 items rendered 0. The grid item's own width IS definite, because it is the track, so putting the ratio **on the item** makes its height definite and the row follows. Related trap on the same page: the site laid the listing out as a masonry using explicit zero-height rows with absolutely positioned children, so overriding only `grid-template-columns` left `grid-template-rows` computing to `0px` — reset **rows and `grid-auto-rows` too**, not just columns. | 2026-09-13 | Put `aspect-ratio` on a child of a grid item in a row sized `auto`, and read the item's `getBoundingClientRect().height` against the child's. Move the ratio to the item and re-read. | `skills/site-redesign/theming.md` |
| `F-COMPUTED-TOP-IS-USED` | **`getComputedStyle(el).top` (and `left`/`right`/`bottom`/`width`/`height`) returns the USED value in pixels, never the keyword `auto`, so an assertion written as `top === "auto"` can never pass — including when the element is behaving exactly as intended.** Measured 2026-09-13: an overlay correctly anchored with `bottom: 0` and no `top` reported `top: 235.617px`, and a spec testing for `"auto"` failed a working element on three pages. Assert the property that actually carries the claim instead: for "anchored at the bottom and growing upward", assert `bottom` is `0px` **and** the element's height is less than its container's. The same rule retires a whole family of specs that test for a keyword the cascade has already resolved. | 2026-09-13 | Absolutely position an element with `bottom: 0` and no `top`, then read `getComputedStyle(el).top`. | `skills/site-redesign/acceptance.md` |
| `F-COMPUTED-STYLE-IS-THE-REMAP` | **When a site's stylesheets are cross-origin, `getComputedStyle` is the only universal colour source — and it is a better one than the CSSOM.** `.cssRules` throws `SecurityError` on a cross-origin sheet, which rules out a rule-level remap entirely; but computed style is readable on **every** element and already folds in inline styles, `<style>` blocks and anything the site's JS set at runtime — exactly the paint a selector-based theme keeps missing. Measured 2026-09-13 on a site with one 1.1 MB cross-origin sheet: a hand-written palette left **9, 7 and 10 light backgrounds** on three page shapes and one whole page in its stock light colours, while a computed-style pass took all three to **zero**. The pass must be **idempotent by construction** so it can run after a static sheet without fighting it: make the test "is this still too light", so a surface the sheet already darkened measures as dark and is skipped; running twice then changes nothing. Cost measured at 96 ms to first paint over 1445 repainted nodes; cap the walk (a few thousand nodes) so a pathological page cannot hang the tab. | 2026-09-13 | On a page with a cross-origin sheet, confirm `document.styleSheets[0].cssRules` throws, then read `getComputedStyle(el).backgroundColor` on the same element and get a usable value. | `skills/site-redesign/theming.md` |
| `F-HUE-SCALE-CANNOT-LIFT-BLACK` | **A colour mapping that preserves hue by scaling the RGB channels by one factor CANNOT lift black, because zero times any factor is still zero — and the failure lands as unreadable text, not as an error.** Channel scaling is the correct way to keep an accent's hue exactly while changing its lightness, but it has a singularity at black. Measured 2026-09-13: a dark-mode pass that lifted ink by scaling left pure-black text sitting on a newly black ground at a contrast ratio of **1.22** on two pages, while every other colour mapped correctly. A colour that dark carries **no hue to preserve**, so the honest mapping for near-black (max channel under ~2%) is a **neutral at the target lightness**, not a scaled copy of itself. The same singularity exists at white for any mapping that scales *down* toward a target. | 2026-09-13 | Take `rgb(0,0,0)`, apply a hue-preserving channel scale toward any target lightness, and read the result — it is still black. | `skills/site-redesign/theming.md` |
| `F-DOCSTART-NEXT-NAV` | **`Page.addScriptToEvaluateOnNewDocument` does not run in the document that is already open — only on the NEXT navigation** — so a runner that registers a userscript and then measures without navigating measures a page with no script at all, and reports stock as the redesign. Measured 2026-09-13 on a local fixture: immediately after the add, `window.__docStartRan` was **false** and the page's own JS reported seeing `site-first`; after one real `Page.navigate` to the same URL the flag was **true**, the injected source observed `readyState: "loading"`, and the page's JS reported `script-first`. The same source sent through `Runtime.evaluate` into the loaded page saw `readyState: "complete"` and `site-first` — the inverted world [F-INJECT-IS-NOT-INSTALL] describes. Register first, navigate second, and never treat the registration itself as an injection. | 2026-09-13 | Serve a page whose own inline script records whether a flag was already set. `Page.addScriptToEvaluateOnNewDocument` a script that sets that flag, read it without navigating (unset), then `Page.navigate` and read it again (set). | `scripts/lib/harness.mjs`, `scripts/redesign-acceptance.mjs`, `skills/site-redesign/acceptance.md` |
| `F-DOCSTART-NO-DOCUMENTELEMENT` | **A script injected by `Page.addScriptToEvaluateOnNewDocument` runs EARLIER than a userscript manager's `@run-at document-start`: `document.documentElement` is still `null`.** So a userscript whose first statement touches `document.documentElement` throws under a CDP document-start probe and works perfectly on install — a false failure that reads like a real defect. Measured 2026-09-13 on a local fixture: at CDP document-start, `document.documentElement === null` was **true**, `document.head`/`document.body` null, `readyState: "loading"`. Wrapping the body so it defers to the first moment `<html>` exists reproduces the manager's timing exactly — same run measured `documentElement` present, `body` still null, `readyState: "loading"`, site JS not yet run, and the attribute it set survived to load. Wrap, do not inject raw. | 2026-09-13 | `Page.addScriptToEvaluateOnNewDocument` a script that records `document.documentElement === null`, navigate, and read the record. Then re-run with the body wrapped in a `MutationObserver` on `document` that waits for `documentElement`. | `scripts/lib/harness.mjs` |
| `F-FOCUS-EMULATION` | **`Emulation.setFocusEmulationEnabled` gives a background tab a live clock WITHOUT bringing its window forward — the same cure as `Target.activateTarget` at none of its cost — and `Page.setWebLifecycleState` does nothing for it.** Measured 2026-09-13, **three runs in a throwaway-profile Chromium 152**, a fresh background target per arm, one probe carrying a 300 ms opacity transition, a `requestAnimationFrame`, an `IntersectionObserver` and a 450 ms sleep. **Untouched background tab:** `hasFocus` false, `visibilityState "hidden"`, transition did **not** tick, rAF 0, IO 0, the 450 ms sleep took 1039-1258 ms. **`Page.setWebLifecycleState {state:'active'}` alone:** identical — false, "hidden", no tick, rAF 0, IO 0, sleep 1352-1372 ms; it can look like it works when measured with focus emulation still applied, so never ship it as half of a pair. **`Emulation.setFocusEmulationEnabled {enabled:true}` alone:** `hasFocus` **true**, `visibilityState "visible"`, transition ticked, rAF 1, IO 1, sleep 451-452 ms. **`Target.activateTarget`:** byte-identical outcome to focus emulation (true/visible/ticked/1/1, sleep 451-452 ms) — so activation is not *wrong*, it is **equivalent and more expensive**: it steals the operator's window, and concurrent agents then fight over which tab is frontmost. Prefer focus emulation. Trusted input lands either way — an independent run the same day registered 1 of 1 `Input.dispatchMouseEvent` clicks on a background, non-activated target 503px wide. `Target.createTarget({background:true})` is accepted and opens the tab without foregrounding it. The measurement-only `*{transition:none!important;animation:none!important}` sheet is still worth having for colour work — separate concern, since a site can ship its own unguarded transition class. Throttle magnitudes are noisy (a first tab in a fresh browser read 628 ms once); the **booleans** are the stable signal. | 2026-09-13 | Create four fresh background targets. On each run one probe reading `document.hasFocus()`, `visibilityState`, a transitioned element's computed value, a rAF count, an IO count and the true duration of a 450 ms sleep — applying, respectively, nothing, `Page.setWebLifecycleState {state:'active'}`, `Emulation.setFocusEmulationEnabled {enabled:true}`, and `Target.activateTarget`. Repeat three times; compare booleans, not millisecond counts. | `scripts/lib/harness.mjs`, `scripts/redesign-acceptance.mjs`, `skills/site-redesign/acceptance.md`, `skills/site-redesign/SKILL.md` |
| `F-LAST-TAB-KILLS-BROWSER` | **Closing the last page target quits Chromium, which takes the debugging port down with it and strands every other agent sharing that browser.** The next call from anyone else fails as a connect error, which reads exactly like a dead rig [F-INPUT-SILENTLY-DROPPED] and gets blamed on the page. Measured 2026-09-13: a probe script that created its own target and closed it at the end left a throwaway-profile Chromium with no page target, and the browser exited — the following run failed at `Target.createTarget` with a socket error. Close only targets you created, and when yours is the last one, navigate it to `about:blank` instead of closing it. | 2026-09-13 | In a browser with one tab, `Target.createTarget`, then `Target.closeTarget` every page target, then probe `/json/version`. | `scripts/lib/harness.mjs` |
| `F-BOOT-LISTENER-SURVIVES-TEARDOWN` | **A teardown that begins `const life = L; if (!life) { return; }` cannot stop a copy that has not started yet, so the "call the previous copy's teardown at entry" contract silently fails for any script that defers `start()` to `DOMContentLoaded`.** The second copy calls the first copy's teardown, the first copy has `L === null` because it is still waiting for the event, teardown returns on its first line without cancelling the pending listener, and the first copy then starts anyway. Measured 2026-09-13 at document-start (`Page.addScriptToEvaluateOnNewDocument` plus a real navigation): **1 copy gave 1 control / 1 scrim / 1 sheet, 2 copies gave 2 / 2 / 2, and 3 copies gave 3 / 3 / 3** — the symptom is **linear in copy count**, which is why a two-copy livelock test can look identical to the bug it is meant to catch and a **three-copy** assertion is the one that proves the fix. The remedy is a module-scope bootstrap `AbortController` whose `abort()` is the **first** statement of teardown, before the `if (!life)` return, with the bootstrap listener registered `{ once: true, signal }`. After the fix all three counts are 1, and a re-run into an already-loaded document stays at 1 **and is still not a no-op** — it must re-apply, or the teardown contract has been traded for an init flag. | 2026-09-13 | Inject the same userscript body three times at document-start on one navigation, then count the controls, scrims and adopted sheets it creates. Any count above 1, or a count that rises with the number of injections, is this bug. | `skills/site-redesign/hardening.md`, `skills/site-redesign/acceptance.md` |
| `F-ELIMINATION-GATE-ONE-CARD` | **A keep-list gate that is satisfied by ONE match is not a gate: the complement rule then hides everything else, and the page ships with a single unit on it.** Hiding by elimination hides *more* as it matches *less*, so the gate is the only thing standing between a rotted keeper test and a blank page — and `keepers.length > 0` passes on exactly the run where the test has rotted off all but one unit. Measured 2026-09-13 on a video site: the gate found **1** organic card, ran the complement, and hid **38 of the 39** units in the container; the shape's own "did the surface render" check passed, because one card genuinely did. Gate on a **plausible fraction of what the container actually holds** — compare the keeper count against the container's child count — and treat a keeper count far below that as a **failed match**, which renders stock. The fraction is not a guess: measured 2026-09-13 on a second site, qualifying shapes scored an organic **share of 0.983 to 1.00** and every non-qualifying shape scored **0**, so the distribution is bimodal with a wide empty gap and a floor anywhere in it separates them. Gate on **share plus a minimum count**, never on `>= 1`. | 2026-09-13 | At document-start, rot the keeper test on all but one unit (rename the attribute or class it keys on), run the redesign, and compare rendered units against the container's `children.length`. 1 of N rendered is this bug; N of N is a gate that held. | `skills/site-redesign/keep-list.md`, `skills/site-redesign/SKILL.md`, `scripts/lib/redesign-checks.mjs` |
| `F-KEEPER-INSIDE-CHROME` | **A keeper routinely lives INSIDE the chrome the brief says to delete, so a container-level removal takes the keeper with it — and the rule looks correct right up until it runs.** Measured on two sites: on a listing site `DIV#header` **contained every relocate candidate**, and the semantic `<header>` sat inside it, so a blanket removal on either would have deleted the adopted overlay along with the chrome; re-confirmed 2026-09-13 on a video site, where the hover-preview wrapper sits **after** the still image in flow rather than inside it, so a rule phrased as "keep the card's first child" keeps the still and drops the preview. Read the keeper's **ancestor chain and its sibling order** before writing any container-level removal, and phrase the removal against the marked path rather than against a named container. | 2026-09-13 | For each keeper, walk `el.parentElement` to `body` and list the chain, then list the keeper's siblings in order. Any node on that chain that also appears on the REMOVE list is this trap. | `skills/site-redesign/keep-list.md`, `skills/site-redesign/relocation.md` |
| `F-SHEET-VS-REPAINT-FIGHT` | **A static override sheet and a computed-style repaint FIGHT when the repaint re-measures a surface the sheet painted: each is right by its own rule and the pair is unreadable.** Measured 2026-09-13: the sheet painted a light accent fill with dark ink; the repaint then darkened that fill — correctly, by its own *"is this still too light"* test [F-COMPUTED-STYLE-IS-THE-REMAP] — and left the ink alone, because ink is repaired against contrast and the pair was never re-measured together. Result **1.21:1** and **2.29:1**. Both were invisible to a contrast audit over the rendered tree because the elements were hidden inside a closed drawer, and surfaced only when that drawer was opened. Idempotence is **not** the fix: it stops the repaint fighting *itself*, not the sheet. Either exclude your own painted surfaces from the repaint — the same single own-UI exclusion list the remap already uses — or **paint none** and let the repaint own every colour. | 2026-09-13 | Paint an element from the static sheet with a light fill and dark ink, run the computed-style repaint over the document, then read both computed colours back and compute the ratio. A darkened fill still carrying the original dark ink is this bug. Run the audit with that element hidden, then visible: only the visible pass reports it. | `skills/site-redesign/theming.md`, `skills/site-redesign/acceptance.md` |
| `F-FOCUS-WITHIN-NOT-A-REVEAL` | **`:focus-within` on a hidden bar is not a keyboard reveal until it is measured to be one.** Measured 2026-09-13 on a video site: an autohiding top bar hidden at rest did **not** reveal when focus moved into it, so the pointer path worked and the keyboard path was dead — a regression no geometry check sees, because at rest the bar measures exactly as intended. The first thing to read is whether anything inside the bar can become `document.activeElement` **at all** while it is hidden: a hide built on `visibility: hidden` or `display: none` takes the whole subtree out of the tab order, so no descendant ever receives focus and `:focus-within` can never match however correct the rule looks. Drive the reveal from a `focusin` listener, which fires wherever focus actually lands, or prove `:focus-within` fires on the live page — never assume it from the selector. | 2026-09-13 | With the bar hidden, call `.focus()` on a control inside it and read `document.activeElement` together with the bar's computed `visibility`/`display`/`translate`. `activeElement` still `BODY` means the subtree is unfocusable and `:focus-within` is dead. Then Tab in from the page and check whether the bar reveals. | `skills/site-redesign/topbar.md`, `scripts/lib/redesign-checks.mjs` |
| `F-HREF-PREFIX-MISSES` | **One kind of card routinely ships under more than one `href` shape, so a PREFIX test on the link silently under-matches while a `contains` test finds them all.** The link target is the strongest machine-checkable signal for "is this card the thing the page is for", which is exactly why a narrow test there is expensive: it does not fail, it quietly shrinks the keeper set, and the elimination gate then hides what it did not keep [F-ELIMINATION-GATE-ONE-CARD]. Measured 2026-09-13 on a video site: the same card type resolved through two different route shapes, a prefix test missed **29** cards on one shape, and the contains test matched every one. **Enumerate the href shapes on every URL shape before writing the test**, and prefer `a[href*="…"]` over a prefix or an exact route. | 2026-09-13 | On each URL shape, tally the distinct `href` patterns of the container's own card links (`new Set([...grid.querySelectorAll('a[href]')].map(a=>new URL(a.href).pathname.split('/')[1]))`) and compare the count your candidate selector matches against the container's child count. | `skills/site-redesign/keep-list.md`, `skills/site-redesign/survey.md` |
| `F-PURGE-IS-LAYERED` | **Purging is layered: removing the chrome you can see EXPOSES chrome you could not, so an enumeration taken once is an enumeration of the first layer only.** Measured 2026-09-13 on a video site: removing three blocks revealed **four more** that the first enumeration could not reach, because they had been sitting below **1800 px** of ad frame and were outside anything a single viewport-relative pass would have looked at. The consequence for the script is a **re-enumeration after every removal** rather than one list applied in one pass; the consequence for verification is that "everything else is gone" must be re-measured **after** each change, not once at the end — which is why the runner's `purge` group counts what still renders rather than checking off a list of selectors. | 2026-09-13 | Enumerate the rendered blocks outside the keep-list, remove them, then enumerate again against the same rule. A second non-empty list is this fact. | `skills/site-redesign/keep-list.md`, `scripts/lib/redesign-checks.mjs` |
| `F-HOVER-STICKS-UNDER-REVEAL` | **When a pointer move reveals an autohiding bar, `:hover` stays on whatever was under the pointer BEFORE the reveal, so `:hover` and `elementFromPoint` disagree until the next move — and a spec that parks the pointer and then reads a rest state measures a HOVER state.** The reveal is a consequence of the move, so it lands after the hit-test that assigned `:hover`; nothing moves again, so nothing re-hit-tests. Measured 2026-09-13 on a local fixture with the bar at a 6 px band: after parking at `(1, 1)`, `document.elementFromPoint(1,1)` returned the **revealed bar** while `document.querySelectorAll(':hover')` returned `html > body > grid > card > a > img` — the card **underneath** — and that card's hover overlay read `opacity: 1` where the spec expected `0`. The failure is deterministic, disappears when the group is run alone (nothing revealed the bar), and looks exactly like a broken `opacity: 0` rule. Pick the element to measure by asking the page (`!el.matches(':hover') && !el.matches(':focus-within')`), never by `elementFromPoint`, and park away from the reveal band. | 2026-09-13 | On a page with an autohiding bar, move the pointer into the reveal band over a hoverable element, then read `elementFromPoint` at that point and `document.querySelectorAll(':hover')` together. They name different elements. | `skills/site-redesign/topbar.md`, `scripts/lib/redesign-checks.mjs` |

## UNVERIFIED — not facts yet

These four are **claims with recipes**, not measurements. Nothing may depend on one: each row names
the shape that ships until it is measured. Promote only by running the recipe and dating it.

| ID | Claim (UNVERIFIED) | Measured | How to re-measure | Cited by |
|---|---|---|---|---|
| `F-UNMEASURED-ESC` | **UNVERIFIED:** whether `Escape` cancels `searchForNode` inspect mode from a non-DevTools client (DevTools' own Esc handling lives in the frontend, which is absent here). Until measured, tier 1 promises **Ctrl-C only**. | — | Arm tier 1 (`page-lab-pick --timeout 45000`), press Escape in the browser, then click an element: if `Overlay.inspectNodeRequested` never fires and a normal click goes through, Escape cancels. **If it fails, leave the operator line as Ctrl-C only.** | `scripts/pick-element.mjs`, `references/routes.md`, `references/pick-protocol.md` |
| `F-UNMEASURED-KAPTURE-AWAIT` | **UNVERIFIED:** that Kapture's `evaluate` (`awaitPromise:true`) blocks on a returned Promise for the full timeout. Tier 3's one-call shape depends on it; the ARM/POLL/DISARM shape is the documented fallback. | — | With the eval toggle on, `mcp__kapture__evaluate({tabId, code:"new Promise(r=>setTimeout(()=>r('late'),10000))", timeout:20000})` and time the call: ≥10 s and `'late'` means it blocks. **If it fails, tier 3 becomes ARM/POLL/DISARM, same shape as tier 4.** | `references/routes.md`, `references/pick-protocol.md`, `scripts/pick-payload.js` |
| `F-UNMEASURED-KAPTURE-HOVER` | **UNVERIFIED:** that `elements({selector:':hover'})` returns the **whole** hover stack. Kapture's tool description says *"Query all elements matching a CSS selector"* while its `selector` param says *"uses first matching element"*. Tier 2's stack-sampling depends on the former. | — | Connect a tab, hover a deeply nested element, then `mcp__kapture__elements({tabId, selector:"%3Ahover"})` and count entries: one means first-match-only. **If it fails, tier 2b demotes to `elementsFromPoint` with operator-supplied coordinates and `routes.md` says so.** | `references/routes.md`, `references/pick-protocol.md` |
| `F-UNMEASURED-DISCONNECT-RESET` | **UNVERIFIED:** whether Chrome auto-resets inspect mode on CDP client disconnect. Do **not** write "the socket close cleans it up" anywhere. | — | Arm one page target with `h_cdp Overlay.setInspectMode '{"mode":"searchForNode","highlightConfig":{...}}'`, let the socket close **without** disarming, then click in that tab: a swallowed click means no auto-reset. Record either way. | `references/pick-protocol.md`, `scripts/pick-watchdog.mjs` |
