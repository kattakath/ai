MCP server declarations, one server.json each, per the MCP registry schema
(modelcontextprotocol/registry, docs/reference/server-json/draft/server.schema.json).

This is the DECLARATION standard. The protocol schema (schema.ts) is a different
file and describes no config at all.
